services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: jps
      POSTGRES_PASSWORD: jps
      POSTGRES_DB: jps
    ports:
      - "5433:5432"
    volumes:
      - jps_pg_data:/var/lib/postgresql/data

volumes:
  jps_pg_data:
